name: Run Bruno CLI Collection with Approval

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
      - develop

jobs:
 use-reusable:
    uses: soltani-a/soltania-devops-github-actions-templates-prototype/.github/workflows/reusable-steps.yml@main
    secrets: inherit  # Hériter des secrets définis dans le dépôt actuel