meta {
  name: 7_Delete_a_comment
  type: http
  seq: 7
}

delete {
  url: {{url}}/{{comment_path_root}}/{{commentId}}
  body: none
  auth: inherit
}

script:pre-request {
  // --- Showcase Strategy: Idempotency & Safety ---
  
  // 1. Self-Healing
  let targetId = bru.getVar("commentId");

  if (!targetId) {
    // Si lancé isolément, on supprime l'ID 1 (sans effet réel sur le mock)
    bru.setVar("commentId", 1);
    console.warn("[Showcase Warning] No commentId to delete. Defaulting to ID 1.");
  } else {
    console.log(`[Showcase Info] Deleting Comment ID: ${targetId}`);
  }
}

tests {
  // 1. Validation de Statut
  test("Status code is 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });

  // 2. Validation de réponse vide
  test("Response body is empty", function() {
    expect(res.getBody()).to.be.an('object');
    expect(Object.keys(res.getBody()).length).to.equal(0);
  });

  // 3. Performance
  test("Deletion took less than 1s", function() {
    expect(res.getResponseTime()).to.be.below(1000);
  });
}

docs {
  # 🗑️ Scenario: Feedback Removal (Cleanup)

  This request concludes the Comment/Feedback lifecycle by removing the resource.

  ## 🧹 Technical Goal: Idempotency & Hygiene
  In a production-grade CI/CD pipeline, tests should ideally be "clean" and not leave orphan data. This request simulates that cleanup process.

  ## 🛡️ Robustness Strategy
  The `pre-request` script implements a **Safe Execution** pattern:
  * **Context Check**: It attempts to retrieve the `commentId` manipulated in the previous `PATCH` step.
  * **Failover**: If the test is running in isolation (no ID in memory), it defaults to `ID: 1`. This ensures the HTTP call completes successfully (Status 200) without breaking the build, even if the specific test context is missing.

  ## ✅ Quality Gates
  * **Status**: 200 OK.
  * **REST Compliance**: Validates that the API acknowledges the deletion by returning an **Empty JSON Object** `{}`, which is the standard confirmation for a successful delete operation.
  * **Performance**: SLA < 1000ms.
}