meta {
  name: Get a list of users by PostID
  type: http
  seq: 4
}

get {
  url: {{url}}/{{user_path_root}}?postId={{postId}}
  body: none
  auth: inherit
}

params:query {
  postId: {{postId}}
}

settings {
  encodeUrl: true
}
